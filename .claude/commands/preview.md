Start the dev server and open a preview window for the portfolio project.

Use the `preview_start` tool with name "portfolio" to start the server, then use `preview_screenshot` to show the current state of the page.
